/**
 * Semver of the core runtime package.
 * Modules declare `engines.core` against this value.
 *
 * Aligned with Module Platform 1.0 (specs/07 version matrix).
 */
export const CORE_VERSION = "1.0.0" as const;
