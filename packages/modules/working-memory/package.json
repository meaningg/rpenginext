{
  "name": "@rpengineext/module-working-memory",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Working memory: last-N chat pairs for narrative + full archive in session state",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@rpengineext/module-sdk": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@rpengineext/contracts": "workspace:*",
    "@rpengineext/core": "workspace:*",
    "@rpengineext/logger": "workspace:*",
    "@types/bun": "latest",
    "typescript": "^5"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}
